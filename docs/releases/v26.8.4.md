# Linker Dashboard v26.8.4

本版本统一更正并联灵巧手的型号名称。

## 主要更新

- 将型号标识从 `L25` 统一更正为 `L20`。
- 同步型号描述、设备识别、手势与手势舞预设、2D 跟随和状态展示。
- 保留 `L20Lite` 型号及其数据不变。

## 下载说明

- Linux 提供 amd64、arm64 压缩包。
- Windows 提供 amd64、arm64 压缩包。
- Android 提供可直接安装的调试签名 APK。
- 使用 `SHA256SUMS` 或随包 `.sha256` 文件校验下载内容。
